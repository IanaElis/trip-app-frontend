import { Alert, Button, Card, Col, Container, Form, Row } from "react-bootstrap";

function LoginForm({form, loading, error, handleChange, handleSubmit}){
    return (
        <div>LoginForm</div>
    );
}

export default LoginForm;